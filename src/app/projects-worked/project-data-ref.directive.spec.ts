import { ProjectDataRefDirective } from './project-data-ref.directive';

describe('ProjectDataRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectDataRefDirective();
    expect(directive).toBeTruthy();
  });
});
